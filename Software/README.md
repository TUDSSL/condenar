# Folder structure

- Console: CMake project for the console firmware. Only needs the arm compiler toolchain to run.
- Logger: Logging module firmware for the ESP32 MCU.
- PCTerminal: C# project that compiles a desktop application (Windows only) useful for debugging purposes.
- StorageManager: C# project that compiles a small console application that allows the upload of files to the console.
